import { AwesomeformfieldDirective } from './awesomeformfield.directive';

describe('AwesomeformfieldDirective', () => {
  it('should create an instance', () => {
    const directive = new AwesomeformfieldDirective();
    expect(directive).toBeTruthy();
  });
});
